<html ng-app="app">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <script src="~/scripts/angular.js" type="text/javascript" language="javascript"></script>
    @*Iniciação do módulo*@
    <script src="~/scripts/App.js" type="text/javascript" language="javascript"></script>
    @*Iniciação do Controllers*@
    <script src="~/scripts/Controllers/EstoqueFisicoCtrl.js" type="text/javascript" language="javascript"></script>
    @*Iniciação do Services*@
    <script src="~/scripts/Services/EstoqueService.js" type="text/javascript" language="javascript"></script>

    <script src="~/scripts/angular-route.js" type="text/javascript" language="javascript"></script>
    <script src="~/scripts/angular-animate.js" type="text/javascript" language="javascript"></script>
    <script src="~/scripts/angular-sanitize.js" type="text/javascript" language="javascript"></script>
    <script src="~/scripts/angular-ui/ui-bootstrap.js" type="text/javascript" language="javascript"></script>
    <script src="~/scripts/angular-ui/ui-bootstrap-tpls.js" type="text/javascript" language="javascript"></script>

    <link rel="stylesheet" href="~/Content/bootstrap.css" />

    <script>

        app.controller("ModalInstanceCtrl", function ($scope, $uibModalInstance, $interval) {
            $scope.ok = function () {
                $uibModalInstance.close();
            };

            $scope.cancel = function () {
                $uibModalInstance.dismiss('cancel');
            };
        });         
    </script>

    <style>
        #btnSearchProd {
            width:500px;
        }

        #btnIncluir {
            float:right;
            margin-bottom: 10px;
        }
    </style>

    <title>Index</title>
</head>
<body ng-controller="estoqueCtrl">
    <div class="container">
        <div class="page-header">
            <h1>Cadastro e Alteração de Estoque</h1>
            <h5 style="width:50%; color:dimgray">Tela responsável pela alteração e inclusão no estoque fisico por produto. Aonde você passa o código do produto e altera o estoque do mesmo se necessário!</h5>
        </div>
        <div>
            <form id="searchForm" class="form-inline">
                <div class="form-group">
                    <input type="text" class="form-control" id="btnSearchProd" ng-model="pesquisar.cdProduto" placeholder="Entre com o identificador do Produto">
                    <input type="button" class="btn btn-primary" value="Procurar" ng-click="Pesquisar();" />
                </div>
            </form>
        </div>

        <div ng-show="estoques.length > 0">
            <button type="button" id="btnIncluir" class="btn btn-info " ng-click="addEstoque()">Add</button>
            <form id="estForm" ng-submit="submitEstoque()">
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>CdProduto</th>
                            <th>Embalagem</th>
                            <th>Qtd.Embalagem</th>
                            <th>Estoque Fisico</th>
                            <th>Estoque Volume</th>
                            <th>Qtd.Pedida</th>
                            <th>Qtd.Comprometida</th>
                            <th></th>
                        </tr>
                    </thead>

                    <tbody>
                        <tr ng-repeat="est in estoques">
                            <td>
                                {{est.CdProduto}}
                            </td>
                            <td>
                                <select class="form-control" id="CdEmbalagem{{$index}}" name="CdEmbalagem{{$index}}" ng-model="est.CdEmbalagem" ng-disabled="!enabledEdit[{{$index}}]">
                                    <option value="{{est.CdEmbalagem}}" selected>{{est.CdEmbalagem}}</option>
                                    <option value="BD" selected>BD</option>
                                    <option value="CX" selected>CX</option>
                                    <option value="DP" selected>DP</option>
                                    <option value="DZ" selected>DZ</option>
                                    <option value="FD" selected>FD</option>
                                    <option value="KG" selected>KG</option>
                                    <option value="SC" selected>SC</option>
                                    <option value="UN" selected>UN</option>
                                </select>
                            </td>
                            <td>
                                <input class="form-control" type="number" id="QtEmbalagem{{$index}}" name="QtEmbalagem{{$index}}" ng-model="est.QtEmbalagem" ng-disabled="!enabledEdit[{{$index}}]" />
                            </td>
                            <td>
                                <input class="form-control" type="number" id="QtEstoqueFisico{{$index}}" name="QtEstoqueFisico{{$index}}" ng-model="est.QtEstoqueFisico" ng-disabled="!enabledEdit[{{$index}}]" />
                            </td>
                            <td>
                                <input class="form-control" type="number" id="QtVolumesFisico{{$index}}" name="QtVolumesFisico{{$index}}" ng-model="est.QtVolumesFisico" ng-disabled="!enabledEdit[{{$index}}]" />
                            </td>
                            @*<td>
                                    <input class="form-control" name="QtPedida{{$index}}" ng-model="est.QtPedida" ng-disabled="!enabledEdit[{{$index}}]" />
                                </td>*@
                            <td>
                                {{est.QtPedida}}
                            </td>
                            <td>
                                {{est.QtComprometida}}
                            </td>
                            <td>
                                <button type="button" class="btn btn-primary" ng-click="editEstoque($index)">Edit</button>
                            </td>
                            @*<td>
                                    <input class="form-control" name="QtComprometida{{$index}}" ng-model="est.QtComprometida" ng-disabled="!enabledEdit[{{$index}}]" />
                                </td>
                                <td>
                                    <button type="button" class="btn btn-primary" ng-click="editEstoque($index)">Edit</button>
                                </td>*@
                        </tr>
                    </tbody>
                </table>
                <input type="submit" class="btn btn-primary" value="Submit" />
            </form>
        </div>
        <h5 class="alert alert-danger" style="width:50%; margin-top: 30px;"><strong>Responsável apenas pelas alterações no estoque do depósito de manilha, a principio </strong></h5>
    </div>
    <script type="text/ng-template" id="myModal.html">
        <div class="modal-header">
            <h3 class="modal-title">Aviso!</h3>
        </div>
        <div class="modal-body">
            Deseja completar a operação?
        </div>
        <div class="modal-footer">
            <button class="btn btn-primary" ng-click="ok()">Sim</button>
            <button class="btn btn-danger" ng-click="cancel()">Não</button>
        </div>
    </script>

    <script type="text/ng-template" id="myModalAlert.html">
        <div class="alert alert-success" style="margin-bottom:auto;">
            Operação feita com <strong>sucesso!</strong>
        </div>
    </script>


</body>

</html>
